const TIMEZONE = 'Asia/Kolkata'; //value must must be in IANA format (refer to https://www.iana.org/time-zones)

const PROJECT_NAME = 'appventurez_js_custom_logger';

module.exports = {
    TIMEZONE,
    PROJECT_NAME,
};
